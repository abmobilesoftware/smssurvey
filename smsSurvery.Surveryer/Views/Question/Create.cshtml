@model smsSurvey.dbInterface.Question

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm("Create", "Question", null, FormMethod.Post, new RouteValueDictionary(new { @class = "form-horizontal" })))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)
   
    <text>
    @{Html.RenderPartial("CreateEditQuestion", Model);}
    </text> 
}

<div>
    @Html.ActionLink("Back to survey", "Edit", "SurveyPlan", new {id = Model.SurveyPlan_Id }, null)
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
