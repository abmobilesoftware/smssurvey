@model smsSurvey.dbInterface.Result

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Result</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Answer)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Answer)
            @Html.ValidationMessageFor(model => model.Answer)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.QuestionId, "Question")
        </div>
        <div class="editor-field">
            @Html.DropDownList("QuestionId", String.Empty)
            @Html.ValidationMessageFor(model => model.QuestionId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.SurveyResultId, "SurveyResult")
        </div>
        <div class="editor-field">
            @Html.DropDownList("SurveyResultId", String.Empty)
            @Html.ValidationMessageFor(model => model.SurveyResultId)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
