@model smsSurvey.dbInterface.QuestionAlertSet

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>QuestionAlertSet</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.Description)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Description)
            @Html.ValidationMessageFor(model => model.Description)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Operator)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Operator)
            @Html.ValidationMessageFor(model => model.Operator)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.TriggerAnswer)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TriggerAnswer)
            @Html.ValidationMessageFor(model => model.TriggerAnswer)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.QuestionId, "QuestionSet")
        </div>
        <div class="editor-field">
            @Html.DropDownList("QuestionId", String.Empty)
            @Html.ValidationMessageFor(model => model.QuestionId)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
